'use client';
import ControlSupportPage from '../manager/support/page';

export default function SupportRolePage() {
  // We reuse the same component built for the manager/owner support panel
  return <ControlSupportPage />;
}
